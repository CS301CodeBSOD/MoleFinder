<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/EditImageViewImage"
        android:layout_width="fill_parent"
        android:layout_height="0dip"
        android:layout_weight="2" >
    </ImageView>

        <RelativeLayout
            android:layout_width="fill_parent"
            android:layout_height="0dip"
            android:layout_weight="1">

            <ImageButton
                android:id="@+id/EditImageViewSubmitButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentRight="true"
                android:contentDescription="Submit"
                android:src="@android:drawable/ic_input_add" />

            <Spinner
                android:id="@+id/EditImageViewDateSpinner"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/EditImageViewSubmitButton" />

            <EditText
                android:id="@+id/EditImageViewNotesField"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_below="@id/EditImageViewSubmitButton"
                android:layout_alignParentBottom="true"
                android:hint="Notes"
                android:inputType="textMultiLine" />
        </RelativeLayout>

</LinearLayout>